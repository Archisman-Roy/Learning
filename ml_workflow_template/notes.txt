# Kaggle competition used in creating this template
https://www.kaggle.com/c/tabular-playground-series-jan-2021


# Run in terminal

git config --global user.name Archisman-Roy
git config --global user.email [valid-email]
git config --list

pip install kaggle

export KAGGLE_USERNAME=royarchi
export KAGGLE_KEY=xxxxxxxxxxxxxx

pip3 install xgboost
pip install lightgbm
pip install plotly==4.14.3
pip install cufflinks==0.17.0
pip install ipywidgets

unzip [file_name]
rm *.zip

python -m create_folds

sh run.sh lightgbmregressor